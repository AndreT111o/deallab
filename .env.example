# Optional — without this, the AI Analyst module (Overview summary, M&A tab
# narrative, and Investment Memo) serves the curated demo analysis bundled
# with Ferrari's data instead of calling OpenAI. Financial calculations
# (DCF, comps, multiples, M&A score) never depend on this key — they are
# fully deterministic.
OPENAI_API_KEY=
